# cpp-string-substring
A simple C++ program demonstrating how to extract a substring from a string using the substr() function.
This project shows a basic example of string manipulation in C++. It uses the substr() function to extract a portion of a string based on a given starting index and length. Ideal for beginners learning C++ string functions.
